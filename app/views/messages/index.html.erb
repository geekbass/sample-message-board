<p id="notice"><%= notice %></p>

<h1>Message Board</h1>

<table>
  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @messages %>

<br>

<%= link_to 'New Message', new_message_path %>
